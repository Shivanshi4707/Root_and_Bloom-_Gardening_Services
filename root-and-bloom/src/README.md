# Source

Active frontend source code for the Root & Bloom customer storefront and manager experience.

- `App.tsx` contains the application shell.
- `main.tsx` mounts React.
- `components/` contains UI features.
- `context/` contains shared state.
- `data/` contains prototype data.
- `services/` contains external service clients.
- `utils/` contains reusable helpers.