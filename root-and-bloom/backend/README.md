# Backend

Private Express API for authentication, manager operations, AI chat, plant advice, and health checks.

Run from this folder's parent with `npm run dev:backend`. It binds to `127.0.0.1:3001` and should not be exposed directly to customers.